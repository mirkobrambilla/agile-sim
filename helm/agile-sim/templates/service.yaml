apiVersion: v1
kind: Service
metadata:
  name: {{ include "agile-sim.fullname" . }}
  labels:
    {{- include "agile-sim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "agile-sim.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
