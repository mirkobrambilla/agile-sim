# Copy this file to `secrets.yaml` in the repo root and replace the placeholder.
# `secrets.yaml` is gitignored — never commit real keys.
#
# OpenRouter: https://openrouter.ai/keys
openrouter_api_key: "your-openrouter-api-key-here"

# Alternative key name (supported by the harness; use one or the other):
# OPENROUTER_API_KEY: "your-openrouter-api-key-here"
